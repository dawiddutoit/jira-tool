# Jira Configuration
#
# Copy this file to .env and fill in your values
# The .env file is gitignored and will not be committed

# Jira Instance URL (REQUIRED)
# Set this to your Jira Cloud instance URL
# Example: https://your-company.atlassian.net
# NOTE: Do NOT use quotes around values - they will be included as part of the value
JIRA_BASE_URL=https://your-company.atlassian.net

# Jira Authentication (REQUIRED)
# Required to interact with Jira API
JIRA_USERNAME=your-email@example.com

# Get the API token from https://id.atlassian.com/manage-profile/security/api-tokens
# To use an environment variable, leave this blank and set JIRA_API_TOKEN in your shell
JIRA_API_TOKEN=your-api-token-here

# Default Project Configuration (OPTIONAL)
# The default project key to use when none is specified in CLI commands
# Example: PROJ, MYPROJECT, DEV, etc.
JIRA_DEFAULT_PROJECT=PROJ

# Default component name (use exact name from Jira, can include spaces)
JIRA_DEFAULT_COMPONENT=
# Epic Link Field ID (OPTIONAL)
# The custom field ID used for linking issues to epics in your Jira instance.
# Common values:
#   - customfield_10014 (Common in many Jira Cloud instances)
#   - customfield_10008 (Alternative)
#   - customfield_10011 (Another alternative)
# If not set, the tool will attempt to auto-detect the epic link field.
# JIRA_EPIC_LINK_FIELD=customfield_10014

# Default Labels
# Comma-separated list of labels to add to created epics by default
# Leave empty or remove this line to add no default labels
# Example: JIRA_DEFAULT_EPIC_LABELS=team,platform,automation
JIRA_DEFAULT_EPIC_LABELS=

# Issue Labels
# Comma-separated list of labels to add to created issues by default
# Leave empty or remove this line to add no default labels
# Example: JIRA_DEFAULT_ISSUE_LABELS=team,automated
JIRA_DEFAULT_ISSUE_LABELS=

# Request Timeout
# Timeout in seconds for HTTP requests to Jira API
# Default: 30
# Increase this if you have slow network or large data transfers
JIRA_REQUEST_TIMEOUT=30

# Retry Configuration
# Maximum number of retry attempts for failed requests
# Default: 3
JIRA_MAX_RETRIES=3

# Display Configuration
# Default number of results to return in searches
# Default: 300
JIRA_DEFAULT_MAX_RESULTS=300
